{
  "name": "line-agent-pun-nattapatch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LINE OA store bot (Worker) — receives webhooks and replies to customers",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "gen:images": "node scripts/gen-images.mjs",
    "test": "node --test \"tests/*.test.js\""
  },
  "dependencies": {
    "@line/bot-sdk": "^11.2.0",
    "express": "^5.2.1"
  }
}
